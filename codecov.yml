coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: 70%
        threshold: 5%

ignore:
  - "examples/**"

comment:
  layout: "condensed_header, condensed_files, condensed_footer"
