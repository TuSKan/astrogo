[project]
name = "astrogo-rv-fixture"
version = "1.0.0"
description = "Generates the Astropy reference table for astrogo's radial-velocity correction"
requires-python = ">=3.11"
dependencies = [
    "astropy>=7.0",
]
